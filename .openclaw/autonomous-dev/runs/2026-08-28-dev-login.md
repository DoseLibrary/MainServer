# Development login fix

## Outcome

- Fresh embedded dev databases now receive an `admin`/`admin` account.
- Dev credentials are configurable before first startup.
- Production setup retains the strong password requirement.
- Setup errors and UI copy now explain the 10-character requirement.

## Verification

- Real PGlite migration, seed, and `admin`/`admin` password login passed.
- `bun run test`: 19 files, 142 tests passed.
- `bun run lint`: passed.
- `bun run verify:production`: passed.

## Review

PASS. Weak default credentials are confined by explicit development and embedded-
database guards and cannot seed Docker/PostgreSQL production deployments.
